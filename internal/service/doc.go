// Package service composes usecases into the coarse-grained surfaces the
// endpoint layer wraps. Services contain NO business logic — they are the
// seam where one wire-facing interface meets many narrow usecases.
package service
