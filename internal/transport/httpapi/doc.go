// Package httpapi is the stdlib net/http surface: everything whose wire
// shape is dictated by an external protocol (OIDC, reverse-proxy forward
// auth, k8s probes, key discovery) rather than by us. No business logic.
package httpapi
