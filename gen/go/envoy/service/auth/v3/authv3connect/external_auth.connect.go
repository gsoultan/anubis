// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: envoy/service/auth/v3/external_auth.proto

// Envoy's external authorization contract, reduced to the parts Anubis
// actually uses.
//
// WHY NOT the upstream protos: they declare
// go_package = "github.com/envoyproxy/go-control-plane/...", so generating
// them would put that module in the import graph, and ADR-0002 keeps the
// direct-dependency list to six. Vendoring the full tree is not much better —
// CheckRequest's transitive closure pulls in most of envoy/config/core,
// envoy/type, xds and udpa annotations, none of which this service reads.
//
// WHAT MAKES THIS SAFE: protobuf compatibility is by FIELD NUMBER, not by
// file. Every message, field number and wire type below was taken from
// envoyproxy/envoy main (api/envoy/service/auth/v3/external_auth.proto,
// attribute_context.proto, config/core/v3/base.proto, type/v3/http_status.proto)
// and is reproduced exactly; fields Anubis ignores are simply absent, and
// proto3 skips unknown fields on the wire. The service name and method name
// must also match exactly, because that is the gRPC path Envoy dials:
// /envoy.service.auth.v3.Authorization/Check.
//
// A wrong field number here would not fail loudly — it would read the wrong
// string and authorize the wrong request — so ext_authz_test.go asserts the
// numbers against this file rather than trusting the comment.
package authv3connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v3 "github.com/gsoultan/anubis/gen/go/envoy/service/auth/v3"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuthorizationName is the fully-qualified name of the Authorization service.
	AuthorizationName = "envoy.service.auth.v3.Authorization"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuthorizationCheckProcedure is the fully-qualified name of the Authorization's Check RPC.
	AuthorizationCheckProcedure = "/envoy.service.auth.v3.Authorization/Check"
)

// AuthorizationClient is a client for the envoy.service.auth.v3.Authorization service.
type AuthorizationClient interface {
	Check(context.Context, *connect.Request[v3.CheckRequest]) (*connect.Response[v3.CheckResponse], error)
}

// NewAuthorizationClient constructs a client for the envoy.service.auth.v3.Authorization service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthorizationClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthorizationClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authorizationMethods := v3.File_envoy_service_auth_v3_external_auth_proto.Services().ByName("Authorization").Methods()
	return &authorizationClient{
		check: connect.NewClient[v3.CheckRequest, v3.CheckResponse](
			httpClient,
			baseURL+AuthorizationCheckProcedure,
			connect.WithSchema(authorizationMethods.ByName("Check")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authorizationClient implements AuthorizationClient.
type authorizationClient struct {
	check *connect.Client[v3.CheckRequest, v3.CheckResponse]
}

// Check calls envoy.service.auth.v3.Authorization.Check.
func (c *authorizationClient) Check(ctx context.Context, req *connect.Request[v3.CheckRequest]) (*connect.Response[v3.CheckResponse], error) {
	return c.check.CallUnary(ctx, req)
}

// AuthorizationHandler is an implementation of the envoy.service.auth.v3.Authorization service.
type AuthorizationHandler interface {
	Check(context.Context, *connect.Request[v3.CheckRequest]) (*connect.Response[v3.CheckResponse], error)
}

// NewAuthorizationHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthorizationHandler(svc AuthorizationHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authorizationMethods := v3.File_envoy_service_auth_v3_external_auth_proto.Services().ByName("Authorization").Methods()
	authorizationCheckHandler := connect.NewUnaryHandler(
		AuthorizationCheckProcedure,
		svc.Check,
		connect.WithSchema(authorizationMethods.ByName("Check")),
		connect.WithHandlerOptions(opts...),
	)
	return "/envoy.service.auth.v3.Authorization/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthorizationCheckProcedure:
			authorizationCheckHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthorizationHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthorizationHandler struct{}

func (UnimplementedAuthorizationHandler) Check(context.Context, *connect.Request[v3.CheckRequest]) (*connect.Response[v3.CheckResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("envoy.service.auth.v3.Authorization.Check is not implemented"))
}
